# CREDENTIAL_MANIFEST.txt — paths included in the encrypted credential vault.
#
# Format: one path per line. Lines starting with `#` and blank lines are ignored.
# Paths may use `~` (expanded to $HOME). Paths may be files OR directories.
# Missing paths are warned, not fatal — manifest is a forward declaration.
#
# To add/remove: edit this file, then run scripts/backup_credentials.sh manually
# to produce a fresh snapshot. The next launchd-triggered run will also pick
# up the change.
#
# See CREDENTIAL_VAULT.md for the threat model, restore runbook, and
# rotation policy.

# ─── Google service-account + OAuth credential JSONs ────────────────────
~/Applications/market_research/credentials/
~/Applications/sentiment_importer/config/
~/Applications/sentiment_importer-donation-mint/config/
~/Applications/truesight_autopilot/config/
~/Applications/video_editor/credentials/

# ─── .env files (API keys, RSA private keys, secrets) ───────────────────
~/Applications/market_research/.env
~/Applications/truesight_me/.env
~/Applications/governor_chatbot_service/.env
~/Applications/video_editor/.env
~/Applications/dao_client/.env
~/Applications/truesight_autopilot/.env
~/Applications/cypher_def/.env
~/Applications/agroverse_shop/.env
~/Applications/garyteh_blog/google-apps-script/.env
~/Applications/openclaw/workshop/.env
~/Applications/tokenomics/python_scripts/schema_validation/.env

# ─── clasp per-account OAuth tokens (Google Apps Script CLI) ────────────
~/.clasprc.json
~/.clasprc-gary.json
~/.clasprc-admin.json

# ─── gh CLI auth (GitHub OAuth) ─────────────────────────────────────────
~/.config/gh/

# ─── SSH (keep id_rsa only; .pub is regeneratable from priv) ────────────
~/.ssh/id_rsa
~/.ssh/id_rsa.pub
~/.ssh/config
~/.ssh/known_hosts

# ─── AWS CLI auth ───────────────────────────────────────────────────────
~/.aws/credentials
~/.aws/config

# ─── Tier-3 candidates (NOT included by default — uncomment if desired) ─
# ~/Library/Application Support/Claude/                 # Claude Desktop / Sonoma config
# ~/.config/anthropic/                                  # any anthropic CLI auth
